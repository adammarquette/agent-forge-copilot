{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Observability": {
    "//": "Ships logs to the local-compose Loki (observability/docker-compose.yml) so they are searchable in Grafana Explore. Fail-open: if Loki isn't running, the sidecar logs to console only. reference: gitlab#107",
    "LokiOtlpEndpoint": "http://localhost:3100/otlp/v1/logs"
  }
}
