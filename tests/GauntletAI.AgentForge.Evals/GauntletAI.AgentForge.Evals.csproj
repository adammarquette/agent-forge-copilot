<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <IsPackable>false</IsPackable>
    <GenerateDocumentationFile>false</GenerateDocumentationFile>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\src\GauntletAI.AgentForge.Documents\GauntletAI.AgentForge.Documents.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- The EvalTests project reuses these internal eval building blocks (GoldenCase, RubricEvaluator,
         Stub/CapturingLogger) so the fast xUnit tests share this gate's exact pipeline. -->
    <InternalsVisibleTo Include="GauntletAI.AgentForge.EvalTests" />
  </ItemGroup>

</Project>
